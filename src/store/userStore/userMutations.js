export default  {
  SET_SEASONS(state, seasons) {
    state.seasons = seasons;
  },
  SET_SEASONS_BY_DATE(state, seasons) {
    state.seasonsByDate = seasons;
  },
  SET_TARIFS(state, tarifs) {
    state.tarifs = tarifs;
  }
}
